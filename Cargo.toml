[workspace]
resolver = "2"
members = [
    "phoenix-v1",
    "perp_router",
]

# Share Phoenix's release profile so verified builds match.
[profile.release]
lto = "fat"
codegen-units = 1
overflow-checks = true
